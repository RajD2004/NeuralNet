# NeuralNet & Weather Data Tools

This repository contains two separate Python programs:

## 1. Neural Network (XOR Demo)
**File:** `NeuralNet.py` :contentReference[oaicite:0]{index=0}  
A simple TensorFlow model that learns the XOR function using a small feed-forward neural network.

Run with:
```bash
python3 NeuralNet.py
